import React from 'react';
import {  Button, MantineProvider} from '@mantine/core';

function Generation() {
    return (
        <MantineProvider>
        <Button>
            Generate (im feel hungry yyummty)
        </Button>
        </MantineProvider>
    );
}

export default Generation

